import React, { Fragment, useMemo, useState, useEffect } from "react";
import {
  TouchableOpacity,
  ScrollView,
  FlatList,
  Image,
  Linking,
  View,
} from "react-native";
import { useDispatch } from "react-redux";

function Index(props) {
  const dispatch = useDispatch();
  let phone = "03211882487";

  return <View></View>;
}

export default Index;
